# VSCode
### Extensions
Install with `cat code/extensions.txt | xargs -L1 code --install-extension`

### User settings
`code/{snippits,keybindings.json,settings.json}` should be placed in:
$HOME/Library/Application\ Support/Code/User/
